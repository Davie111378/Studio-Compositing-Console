# A 组 · 文字输入 Agent 一周连续冲刺计划 V1.0

> 目标：**7 天（09-09 晚 ~ 09-16）内机器连续不停转，把文字输入 Agent 做到可交付 P0 状态**
> 提前于 09-18/23/25 的各节点，且领先 09-20 生死线 2（语音→Agent→DAG→工具→图像贯通），为语音/空间指代预留接口
> 参考：《A组_文字输入Agent完善任务清单_V1.0.md》（完整版，本文件只做压缩与排期）
> 压缩策略：**砍 P1/P2 + 白天人工 coding + 夜间批量脚本跑满机器 + 可批量项下沉为脚本**

---

## 0. 压缩原则与范围裁剪

| 原任务 | 处置 | 理由 |
|---|---|---|
| A1 8 原子工具 Schema | **保留（唯一硬前置，Day1）** | Planner/Executor 全依赖它 |
| A3 Executor DAG | **保留，裁到最小可用**：topo + Retry/Skip/Re-run + 节点幂等 + 产物表；**砍 Pause/Resume 的 UI 语义**（仅保留进程级 stop/continue） | 满足 5 轮不崩即可 |
| A2 Planner JSON DAG | **保留**：强校验 + 重试≤2 + 关键词模板兜底；指标达标用**夜间批量 LLM 扫描自动选 few-shot** | 创新三核心 |
| A4 Critic 结构化 | **保留但压缩**：5 维解析 + 最低维→工具 + re-plan 闭环；阈值用 30 组**夜间批量标定**，不与主程抢时间 | 主链路第 4 环 |
| A5 条件回滚 | **保留**：节点级版本化 + "换回背景A保留光" + 下游重跑 | 创新三最重点 Demo04 |
| A8 日志/多轮 | **保留最小版**：结构化回放日志 + 5 轮脚本 | 验收必需 |
| 两级出图 + ≤10s | **保留**：draft/fine，抠图与背景生成并行 | ≤10s 指标 |
| A6 Spring Boot API | **砍**（本迭代不建后端，Agent 以本地服务/CLI 进程形式交付） | 非 P0，不阻塞 Demo |
| A7 异步任务队列 | **砍**（后置，Web 集成时再做） | 非 P0 |
| A9 Docker 部署 | **降 P2**：只产 `requirements-lock.txt` + 启动脚本，docker-compose 视剩余时间 | 09-29 之前都有缓冲 |
| greenscreen / apply_fx / remove_region / video | **收敛并入 8 原子**（→T01/T07/T08），不单独立工具 | 工具≤8 保 Planner 准确率 |
| Critic 可信度三指标（Spearman 等） | **降到"记录原始分"，达标验证放 W5** | 不阻塞 07/16 |

> 裁剪后 P0 核心 = **Schema → DAG Executor → Planner → 真实工具适配 → Critic闭环 → 条件回滚 → 贯通成片**，正好对齐 4 个 Demo 剧本链路。

---

## 1. 排期总览（逐日，白天/夜间双轨）

> 标记：[A]=我在线 coding/联调；[B]=夜间批量脚本（跑满机器，无人值守，次日早取结果）
> 每日节奏：白天推进当前模块；收工前启动当夜 [B] 任务，次日先看结果再改。
> **守则：夜间机器绝不空转** —— 当日无 [B] 任务也跑单元回归/冒烟/日志复现。

| 日 | 白天 [A] 主线 | 夜间 [B] 批量（次日早取结果） | 当日验收门 |
|---|---|---|---|
| **D1**(09-09晚-10) | ①git/main保护/LFS/README ②抽硬编码 Key(.env) ③定 8 原子编号表 ④写完 `agent/schema/T01-08.json` 5️⃣评审 | [B1] 由 Schema 自动生成 **100 条指令测试集脚本**（覆盖 8 工具/多轮/指代参数）并跑出骨架 json | 🔴 **A1 8 份 Schema 全冻结（09-09 硬截止达成）** |
| **D2**(09-11) | ⑥`dag.py` 数据模型 + 拓扑排序 ⑦`engine.py` 状态机(pending/running/done/failed)+Retry/Skip/Re-run ⑧`artifact.py` 产物表 ⑨适配器映射 8 原子→`ai-service/src/*` | [B2] 真实 `matting+composite` **批量热身跑**：彩排 DAG 产物落盘 + 记录每节点真实 latency（供≤10s 用） | Executor 能解析规范示例 DAG 并真实跑通 matting→composite |
| **D3**(09-12) | ⑩`planner/prompt.py` 输出 JSON DAG + few-shot ⑪强校验器(jsonschema)+失败重试≤2 ⑫`fallback.py` 关键词模板兜底 | [B3] **100 条指令全量 Planner 扫描**：批量调 LLM 出 DAG，凌晨自动出 3 档 few-shot 的 Tool/Order/Param 分数对比 | Planner 单条真机出合法 DAG ≤3s |
| **D4**(09-13) | ⑬集成 mock→真工具 **全链路草稿**（一句话→DAG→成片≲10s）⑭draft/fine 两档 ⑮抠图·背景生成并行 ⑯延迟采样 | [B4] 用昨日选优 few-shot，**夜间再扫 100 条**定版 + 批量重跑 DAG 落中间产物（喂给 C5 DAG 可视化） | **主干贯通**：说一句中文能出真实成片并稳定 ≤10s |
| **D5**(09-14) | ⑰`critic/parse.py` 5维结构化 + 最低维→工具 ⑱re-plan 闭环(重跑该+下游,≤2次) ⑲`eval/log.py` 可回放日志 ⑳`multi_round.py` 5 轮脚本 | [B5] **30 组样本 Critic 批量标定**：成片批量打分，出阈值建议 + 与人工分对照（记录，不强达标） | Critic 能自动定位最低维并触发重跑；5 轮脚本无崩溃 |
| **D6**(09-15) | ㉑`rollback/version.py` 节点级版本化 ㉒`tree.py` 编辑历史树 + 条件回滚 ㉓Demo04"换回背景A保留光"场景脚本 | [B6] 全量回归：D1-D5 所有模块一次跑通回归套装 + 导出日志数据集 | 🔴 **条件回滚 100% 通过**；C4 5 轮剧本全过 |
| **D7**(09-16) | ㉔与 C 组锁**输入契约**（文本/语音转文本/点击坐标，Agent 接口零改动可就接）㉕README/启动脚本/`requirements-lock.txt` ㉖全流程冒烟 + 录制 Demo01/03 链路 ㉗(有余力)A9 docker-compose | [B7] 最终全回归 + 延迟复测确认 ≤10s，生成交付包 | 🔴 **可交付 P0**：一句话→DAG→真实成片→Critic→回滚全部演示可跑 |

---

## 2. 各日预计投入（人日 + 机时）

| 日 | 白天人工 | 夜间机时 | 说明 |
|---|---:|---:|---|
| D1 | 1 AD | 6h | 纯手工写 Schema，机时少 |
| D2 | 1 AD | 6h | Executor 编码为主 |
| D3 | 1 AD | 8h(通宵 LLM 扫描) | 批量扫 few-shot 是省时关键 |
| D4 | 1 AD | 6h | 集成联调 |
| D5 | 1 AD | 6h | Critic 标定批量 |
| D6 | 1 AD | 5h | 回滚实现 + 回归 |
| D7 | 0.5 AD | 4h | 交收 |
| **合计** | **≈6.5 AD** | **≈41h 机时** | 机器连续运转 ≈ **172h/7天** |

> 白天人工 ≈6.5 个有效生产日，夜间批量 ≈41h 把"评测集构建、few-shot 选优、Critic 标定、全量回归"这些重活**剥离成脚本跑满机器**——这就是从 14~22 人日压到 7 天的关键。

---

## 3. 夜间批量脚本清单（[B1]–[B7]，需提前写好被调执行器）

| 脚本 | 产出 | 跑法 |
|---|---|---|
| B1 `gen_instructions.py` | 100 条指令 json + 期望工具/依赖/参数标注模板 | D1 夜触发，全自动 |
| B2 `warm_pipeline.py` | 真实 matting+composite 热身、逐节点 latency 表 | D2 夜，`outputs/agent/` 落盘 |
| B3 `planner_sweep.py` | 3 组 few-shot × 100 条 = 300 次 LLM 调用 → 指标对比 CSV | D3 夜，定时批量（限速防 429） |
| B4 `planner_final.py` + `dag_batch.py` | 定版 DAG 达标注 + 批量重跑落中间产物 | D4 夜 |
| B5 `critic_calib.py` | 30 组成片批量打分 → 阈值建议 | D5 夜 |
| B6 `regression.py` | D1–D5 全模块回归套装 + 导出日志数据集 | D6 夜 |
| B7 `final_regression.py` | 全回归 + ≤10s 复测 + 交付包 | D7 夜 |

---

## 4. 压缩后仍保留的硬指标（够 Demo，不恋战）

- 🔴 Tool Selection ≥95% / DAG Order ≥95% / Parameter ≥90%（D3/D4 夜间扫到分即存档，**不追求反复调参**）
- 🔴 草稿端到端 ≤10s / 精修 ≤30s（D4 实测）
- 🔴 5 轮剧本不崩溃（D5）、条件回滚 100%（D6）
- ◐ Critic Spearman≥0.6 等三级指标 → **W5（09-26-29）正式验收**，本迭代只留原始分

## 5. 风险与兜底（7 天更紧）

| 风险 | 应对 |
|---|---|
| D3 通宵 LLM 批量偶发 429/限流 | `agnes_client` 已有重试；扫描脚本加限速与断点续扫（失败项重跑而非全跑） |
| D2 真实工具适配卡 OOM/耗时 | draft 档降分辨率；抠图与背景生成并行；必要时先用 `studio_cli` 高层函数包住原子节点（保功能再拆） |
| 7 天无缓冲，某事延期超过 1 天 | 启用**里程碑降级**：先砍 ⑭ 精修档（Demo 只用草稿）→ 再砍 ㉗ Docker；核心 4 条 Demo 链路优先级最高 |
| API Key/网络在演示环境不可用 | 保留 `run_cli.py` 纯文本 + 模板 DAG 降级（规范 L2）；Key 已抽环境变量 |

---

## 6. 一句话行动指令（立即开始）

1. **现在**初始化 git + 抽 Key + 写 `agent/schema/` 8 份（Day1 白天）。
2. **收工前**把 [B1] 脚本写好并于当晚触发，让机器先转起来。
3. 之后每日"白天 coding 当前模块 → 收工触发当夜 [B] → 次日看结果改"循环推进。
4. **每日站会只对外报 3 句**：昨天完成的门 / 今天在做的 / 阻塞（对齐 C 组输入契约在 D7 前说清）。

> 已按此压缩重排；要不要我**现在就开始 Day1**：建 git + 抽硬编码 Key + 起草 8 份 `agent/schema/T01–T08.json`？