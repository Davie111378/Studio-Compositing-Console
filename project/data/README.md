# data（B 组：数据集，09-09 冻结）

- 子目录：`matting/`（四集划分 train/val/test/hard-case）、`composition/`、`lighting/`、`instruction/`（≥100 条测试指令集，A2 评测用）
- `artifacts/` 与 `runs/` 是 Agent 服务运行产物（上传图、节点产物、会话状态、回放记录），已 gitignore，**不入库**。
- 数据清单、来源与授权写在本目录 `README` 或 `DATA_MANIFEST.md`（B2 交付）。
