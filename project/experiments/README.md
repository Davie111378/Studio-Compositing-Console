# experiments（B 组：实验记录，规范 N5）

- 子目录：`baseline/`（B1）、`ours/`、`metrics/`、`visualization/`、`failure/`（B8，≥5 组+归因，正式交付物）、`user_study/`（P4）
- 每个实验目录 = `config.yaml + train.log + eval.csv + metrics.json + visual/ + README.md`（附录 B 模板）
- `exp_id` 命名：`YYYYMMDD-<方向>-<序号>`，如 `20260912-matting-01`
